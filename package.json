{
  "name": "sikamitra",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && xcopy /E /I frontend\\dist dist" 
  }
}
