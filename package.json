{
  "name": "sikamitra",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node -e \"fs.cpSync('frontend/dist', 'dist', {recursive: true})\""
  }
}